# 写作指南

用 VS Code 写文章。打开 `E:\MyBlog` 文件夹（不是单个文件），下面的功能才生效。

## 一、新建文章

按 `Ctrl+Shift+P`，输入 `Tasks: Run Task`（中文版是 `任务: 运行任务`），选 **新建文章**，输入标题回车。

命令行方式也行：

```bash
npx hexo new "文章标题"
```

生成的文件在 `source/_posts/` 下，顶部会自动带好这段：

```markdown
---
title: 文章标题
date: 2026-09-20 23:20:00
tags:
categories:
---
```

`tags` 和 `categories` 按需填，不填也能发。想要显示在首页摘要里，正文开头写一段话即可（摘要自动截取）。

## 二、插图（最省事的一步）

1. 截图（`Win+Shift+S`）或复制任意图片
2. 光标停在想插图的位置
3. 按 **`Ctrl+Alt+V`**

图片会自动存到 `source/img/`，文件名带文章名前缀（如 `hello-world-2026-09-20-23-30-00.png`），正文里自动插入：

```markdown
![](/img/hello-world-2026-09-20-23-30-00.png)
```

不用手动拖文件、不用填路径。想调图片大小，Hexo 里一般靠 CSS，或者用 HTML：

```html
<img src="/img/xxx.png" width="60%">
```

## 三、边写边看

- **`Ctrl+K` 然后按 `V`** —— 右侧分屏预览，左边写右边即时刷新
- **`Ctrl+Shift+V`** —— 单独标签页打开预览

## 四、Markdown 速查

| 想写 | 写法 |
| --- | --- |
| 一级标题 | `# 标题`（文章里从 `##` 开始用） |
| 二级标题 | `## 标题` |
| 加粗 | `**文字**` |
| 斜体 | `*文字*` |
| 链接 | `[文字](https://example.com)` |
| 无序列表 | `- 内容` |
| 有序列表 | `1. 内容` |
| 引用 | `> 引用内容` |
| 行内代码 | `` `代码` `` |
| 代码块 | 三个反引号包起来，可在后面标语言如 `javascript` |
| 分割线 | `---` |
| 表格 | `\| 列1 \| 列2 \|` 换行 `\| --- \| --- \|` |
| 删除线 | `~~文字~~` |
| 任务清单 | `- [ ] 待办` / `- [x] 完成` |

Markdown All in One 提供的快捷键：`Ctrl+B` 加粗、`Ctrl+I` 斜体、`Ctrl+Shift+]` 提升标题等级。

## 五、本地预览整站

`Ctrl+Shift+P` → `Tasks: Run Task` → **本地预览**，然后浏览器打开 http://localhost:4000 。

或者命令行：

```bash
npm run server
```

停止：在终端里按 `Ctrl+C`。

## 六、发布

`Ctrl+Shift+P` → `Tasks: Run Task` → **发布到 GitHub**，输入一句提交说明即可。

等同于手动执行：

```bash
git add -A
git commit -m "写完了某篇文章"
git push
```

推送后 GitHub Actions 会自动构建，约 1–2 分钟后线上生效。

## 七、几个注意点

- **文章里的日期是 front-matter 里那个 `date`**，不是文件修改时间，想改展示顺序就改它
- 标题里有英文冒号 `:` 时，命令行新建会因为解析问题出错，改成中文冒号或去掉
- 草稿：`npx hexo new draft "标题"` 放在 `source/_drafts/`，不会发布；定稿用 `npx hexo publish "标题"`
- 图片尽量别超过 500KB，公众号截图建议先压一下，否则仓库会越来越大
- 想给文章置顶，在 front-matter 里加 `sticky: 1`，数字越大越靠前

## 八、把外面已有的 md 导入博客

从笔记工具（语雀、飞书、Typora 等）导出的 md，直接丢进 `source/_posts/` 发不出去，缺两样东西。

### 1. 补 front-matter（必须有）

打开文件，在**最顶端**插入：

```markdown
---
title: 文章标题
date: 2026-09-20 23:47:00
tags:
  - 标签
categories:
  - 分类
---
```

三条横线要顶格，原文正文接在第二组 `---` 下面的空行之后。

> 更省事的办法：先 `npx hexo new "文章标题"` 生成骨架，再把外部 md 的正文（不包含它自带的 front-matter）粘进去。格式绝对不会错。

### 2. 图片必须搬出 `_posts`

**`source/_posts/` 下的非 md 文件会被 Hexo 完全忽略**，图片放这里会全部裂掉，而且构建时不会报任何错。

```bash
# 图片移到 source/assets/
mv source/_posts/assets/* source/assets/

# 把引用从 ](assets/ 批量改成 ](/assets/
```

改完记得 `npx hexo clean && npx hexo generate`，然后验证图片真的进了产物：

```bash
find public -name "*.png" | head
```

一张都找不到就说明路径还没对。

### 3. 检查花括号

正文里有 `{{ }}` 或 `{% %}` 的话，Hexo 会当模板语法解析然后报错。用这个包起来：

```markdown
{% raw %}
这里写 {{ 什么 }} 都不会被解析
{% endraw %}
```

### 4. 发布

```bash
git add -A
git commit -m "post: 文章标题"
git push
```

或者 VS Code 里跑 **发布到 GitHub** 任务。

### 一句话版

**复制 md 进 `source/_posts/` → 补 front-matter → 图片移到 `source/assets/` 并把引用改成 `/assets/` → push。**

漏掉前面任何一步，push 上去都看不到或者全是裂图。
